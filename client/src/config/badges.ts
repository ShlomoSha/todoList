const BADGE_COLORS: Record<string, { bg: string; color: string }> = {
  total:   { bg: '#c8a98a', color: '#fff' },
  pending: { bg: '#e07b5a', color: '#fff' },
  done:    { bg: '#7aab8a', color: '#fff' },
};

export default BADGE_COLORS